language: python
python:
  - "3.7-dev"

env:
  - DJANGO=2.0.7 DB=postgre

install:
  - pip install -q Django==$DJANGO --use-mirrors
  - pip install -q . --use-mirrors
  - pip install -q -r requirements.txt --use-mirrors

script: python manage.py test